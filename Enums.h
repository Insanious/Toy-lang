#ifndef ENUMS_H
#define ENUMS_H

enum class ExprType { FUNCTIONCALL, IDENTIFIER, DEFINITION, ASSIGNMENT, CONSTANT, BINOP, IFSTMT, FORLOOP };
enum class DataType { INT, BOOL, FUNCTION, IDENTIFIER };
enum class BinOp { EQUALS };

#endif
